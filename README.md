# Web Chiaseanh

A minimal static website scaffold created to initialize the empty workspace.

## Files
- `index.html` — starter page
- `styles.css` — basic styling
- `script.js` — simple interactivity

## Getting Started
Open `index.html` in a browser to preview the site.

## Next Steps
- Add your own content to `index.html`
- Customize styles in `styles.css`
- Extend functionality in `script.js`
